<Window x:Class="KSubMaker.App.Views.TextPromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:res="clr-namespace:KSubMaker.App.Resources"
        Height="200"
        Width="480"
        MinHeight="180"
        MinWidth="360"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="CanResize"
        FontFamily="Malgun Gothic"
        FontSize="12">

    <DockPanel LastChildFill="True" Margin="14">

        <TextBlock DockPanel.Dock="Top"
                   x:Name="MessageText"
                   TextWrapping="Wrap"
                   Margin="0,0,0,8" />

        <StackPanel DockPanel.Dock="Bottom"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">
            <Button Content="{x:Static res:Strings.Ok}"
                    Style="{StaticResource CommandButton}"
                    IsDefault="True"
                    FontWeight="SemiBold"
                    Click="OnAccept" />
            <Button Content="{x:Static res:Strings.Cancel}"
                    Style="{StaticResource CommandButton}"
                    IsCancel="True" />
        </StackPanel>

        <TextBox x:Name="Input"
                 VerticalContentAlignment="Top"
                 AcceptsReturn="False"
                 Padding="5,4"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto" />

    </DockPanel>
</Window>
